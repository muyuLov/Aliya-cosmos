# 2026-07-19

## 生成生产级 Docker 部署配置（Aliya-cosmos）
- 新增文件：`Dockerfile`（多阶段 builder+runtime，uv 锁版本，`libportaudio2`/`ffmpeg`/`tini`，非 root 用户 uid 1001，TCP 端口健康检查，tini 作 PID1）、`.dockerignore`、`docker-compose.yml`（base：app+neo4j+可选 astratts profile）、`docker-compose.override.yml`（dev：bind mount 源码热重载）、`docker-compose.prod.yml`（prod：只读根fs+tmpfs、资源限制、cap_drop ALL、no-new-privileges、命名卷）。
- 修改 `data/config/main.yml`：新增 `cosmos.service.agent.ws_server.host/port`（env `${WS_HOST:127.0.0.1}`/`${WS_PORT:8765}`），Neo4j uri 改为 `bolt://${NEO4J_HOST:127.0.0.1}:${NEO4J_PORT:7687}` —— 本地默认不变，容器内由 compose 覆盖（WS 绑定 0.0.0.0、Neo4j 用服务名）。
- 扩充 `.env.example`：新增 WS_HOST/WS_PORT/NEO4J_HOST/端口/ASTRA_PORT/APP_PORT。
- 校验：`docker compose config -q` 对 dev(prod+override) 与 prod(base+prod) 均 VALID。Docker 守护进程 v29.4，但 `--check` 因网络慢未跑完整构建。
- 关键约束：`sounddevice` 在 `core/tts/player/core.py` 顶层导入 → 运行时必须 `libportaudio2`；默认 TTS 为 edge（联网，无需容器）；`data/AstraTTS` 不存在 → astratts 设为可选 profile，需用户自备 Dockerfile 才启用；`.env` 含真实密钥，已被 .dockerignore 排除、不烘焙进镜像。
- 启动：dev `docker compose up`；prod `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d`。

## 优化 Dockerfile / docker-compose.yml（第二轮）
- Dockerfile 修复一个会导致构建异常的 bug：`ENV` 续行中间插入 `#` 注释会破坏变量解析 → 拆分为两个 `ENV`（通用 + 应用默认值）。
- 依赖分层缓存：`uv sync --frozen --no-dev --no-install-project` 先只装三方依赖（仅依赖 pyproject+uv.lock 层），再 COPY 源码，再 `uv sync` 装项目 → 改代码不再触发整依赖重装。
- 精简 builder：去掉 slim 已带的 `ca-certificates` 与锁文件构建不需要的 `git`，仅留 `build-essential`。
- 健康检查合并单行。
- docker-compose.yml：Neo4j 版本固定 `neo4j:5.26` + 堆内存 `256m/512m` + deploy 资源上限（cpu 1.0 / mem 1g）；健康检查改 `curl -fsS`（镜像自带，wget 兜底）；app 与 neo4j 增加 `stop_grace_period: 30s`。
- `.dockerignore` 收紧：排除 `.codebuddy`/`.codegraph`/`*.egg-info`/`CLAUDE.md` 等噪声。
- 校验：dev / prod 两套 `docker compose config -q` 仍 VALID。
